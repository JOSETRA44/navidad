
  # Cuenta Regresiva con Mensaje

  This is a code bundle for Cuenta Regresiva con Mensaje. The original project is available at https://www.figma.com/design/aHkad9JpfmowhkDAbnjABK/Cuenta-Regresiva-con-Mensaje.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  